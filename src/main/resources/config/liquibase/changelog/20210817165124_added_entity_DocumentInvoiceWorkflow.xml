<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:ext="http://www.liquibase.org/xml/ns/dbchangelog-ext"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd
                        http://www.liquibase.org/xml/ns/dbchangelog-ext http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-ext.xsd">

    <!--
        Added the entity DocumentInvoiceWorkflow.
    -->
    <changeSet id="20210817165124-1" author="jhipster">
        <createTable tableName="document_invoice_workflow" remarks="Defining the workflow of the invoice with  R - payment reminder, R1 - 1st reminder, R2 - 2nd reminder, R3 - 3rd reminder">
            <column name="id" type="bigint" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="active" type="boolean" remarks="whether workflow is active">
                <constraints nullable="true" />
            </column>
            <column name="status" type="varchar(255)" remarks="status of a document that the workflow is for; possible values: R - payment reminder, R1 - 1st reminder, R2 - 2nd reminder, R3 - 3rd reminder">
                <constraints nullable="true" />
            </column>
            <column name="overdue_days" type="integer" remarks="how many days after due date action should be taken ,">
                <constraints nullable="true" />
            </column>
            <column name="action" type="varchar(255)" remarks="action that should be taken; possible values: M - remind me, CE - remind contact by email, CP - remind contact by post">
                <constraints nullable="true" />
            </column>
            <column name="contact_email" type="varchar(255)" remarks="email address for reminding contact (valid only when action is CE)">
                <constraints nullable="true" />
            </column>
            <column name="speed" type="varchar(255)" remarks="post speed for reminding contact (valid only when action is CP); possible values: P - priority, E - economy">
                <constraints nullable="true" />
            </column>
            <!-- jhipster-needle-liquibase-add-column - JHipster will add columns here -->
        </createTable>
    </changeSet>

    <!-- jhipster-needle-liquibase-add-changeset - JHipster will add changesets here -->

    <!--
        Load sample data generated with Faker.js
        - This data can be easily edited using a CSV editor (or even MS Excel) and
          is located in the 'src/main/resources/config/liquibase/fake-data' directory
        - By default this data is applied when running with the JHipster 'dev' profile.
          This can be customized by adding or removing 'faker' in the 'spring.liquibase.contexts'
          Spring Boot configuration key.
    -->
    <changeSet id="20210817165124-1-data" author="jhipster" context="faker">
        <loadData
                  file="config/liquibase/fake-data/document_invoice_workflow.csv"
                  separator=";"
                  tableName="document_invoice_workflow">
            <column name="id" type="numeric"/>
            <column name="active" type="boolean"/>
            <column name="status" type="string"/>
            <column name="overdue_days" type="numeric"/>
            <column name="action" type="string"/>
            <column name="contact_email" type="string"/>
            <column name="speed" type="string"/>
            <!-- jhipster-needle-liquibase-add-loadcolumn - JHipster (and/or extensions) can add load columns here -->
        </loadData>
    </changeSet>
</databaseChangeLog>
